var gulp = require('gulp');
var cache = require('gulp-cached');

var config = require('../config.js');

gulp.task('cache', function(tasks) {
    console.log(taskss);
    for (var i = 0; i < len; i ++) {
        cache(tasks[i]);
    }
});